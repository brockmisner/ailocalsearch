import './globals.css'
export const metadata={title:'Stakeout Local Rank Tracker',description:'Real Android local rank tracking'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
